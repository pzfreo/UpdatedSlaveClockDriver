# UpdatedSlaveClockDriver
A driver for a master/slave clock based on NTP with WifiManager


The clock has a DRV motor controller on D0,D1 with SLeep on D2. D4 is attached to a button to start the config portal